print("""

Intro Computer Architecture 

- menu - 

let i = 1; 
while (i < 15) {
      i * 2
}
open(data.txt)
write(i)
close(data.txt)

pick one. 
0)
1)What part of the code is the input?
2)What part of the code is the process?
3)What part of the code is the output?
4)Does it utilize feedback? how?
5)Does it utilize storage? how?
""")
answer = "empty"
userchoice = input()

while(answer == "empty"):
  answer = "chosen"
if(userchoice == "1"):
  print(""" 'i' is the input """)
elif(userchoice == "2"):
  print(""" 'open(data.txt)
  write(i)
  close(data.txt)'""")
elif(userchoice == "3"):
  print(""" 'write (i)''""")
elif(userchoice == "4"):
  print("""
  the code uses feedback by administaring i seeing if it is less than 15.""")
elif(userchoice == "5"):
  print("""no the code does not use storage """)
elif(userchoice == "0"):
  print("""IPO is a basic acronym representing basics of an information system. Let's look more into what this means exactly.""")
